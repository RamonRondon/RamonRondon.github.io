{
  "name": "portfolio-ramon-rondon",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/generate-posts.js --dev && vite",
    "build": "vite build && node scripts/generate-posts.js && node scripts/generate-sitemap.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "jimp": "^1.6.1",
    "marked": "^18.0.5",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "qrcode-generator": "^2.0.4"
  }
}
